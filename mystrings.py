# We  can define strings in single, double and triple qoutes(for multilines)
# Singe its a collection of char we can access them indiviually using [index]
# It allows negative indexing
# Slicing works by [: ]

# firstName = "Jhon"
# lastName = "Smith"
# print(f"{firstName} [{lastName}] is a coder")
course = "Python for Beginners"
print(course.replace("P", "J"))
print("Jython" in course)
